#!/bin/bash

set -e

# Ensure dos2unix is installed
if ! dpkg -l | grep -q dos2unix; then
    echo "dos2unix is required but not installed. Installing..."
    apt-get update
    apt-get install -y dos2unix
fi
