#!/bin/bash

set -e

# Convert any text files to Unix format
echo "Converting text files to Unix format..."
find /usr/bin -type f -exec dos2unix {} \;

# Ensure paramiko is installed
echo "Ensuring paramiko is installed..."
pip3 install paramiko

echo "Configuration complete!"